[tool.poetry]
name = "matrix"
version = "1.0"
description = "I need guns. Lots of guns."
authors = ["nkerstin"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
numpy = "^1.25.0"
matplotlib = "^3.7.2"